#!/bin/bash
#
# gen_groups.sh outputs the required hadoop groups -- of which there is
# currently only one. This is the only source for these groups. No attempt is
# made to create nor validate these groups.

echo "hadoop"
